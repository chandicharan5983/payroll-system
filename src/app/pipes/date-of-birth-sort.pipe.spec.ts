import { DateOfBirthSortPipe } from './date-of-birth-sort.pipe';

describe('DateOfBirthSortPipe', () => {
  it('create an instance', () => {
    const pipe = new DateOfBirthSortPipe();
    expect(pipe).toBeTruthy();
  });
});
